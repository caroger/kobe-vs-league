{
  "name": "kobe-vs-league",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "homepage": "https://caroger.github.io/kobe-vs-league/",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx webpack -w --mode=development",
    "predeploy": "npm run build",
    "build": "webpack --watch --mode=development",
    "deploy": "npm run build && gh-pages -d dist",
    "pages": "gh-pages -d dist"
  },
  "dependencies": {
    "d3": "^5.6.0",
    "lodash": "^4.17.21",
    "topojson": "^3.0.2"
  },
  "devDependencies": {
    "@babel/core": "^7.13.8",
    "@babel/preset-env": "^7.13.9",
    "babel-loader": "^8.2.2",
    "webpack": "^4.46.0",
    "webpack-cli": "^4.5.0",
    "gh-pages": "^2.2.0"
  }
}
